"use client";

import { CaseDetailsForm } from "@/features/cases/components/case-details-form";

/**
 * Renders the primary form for capturing the details of an analysis case.
 * This component serves as the entry point for the case creation workflow.
 */
export const AnalyzeDetails = () => {
  return <CaseDetailsForm />;
};
