"use client";

/**
 * The content display component for the account settings page.
 * It will render the forms and information corresponding to the selected tab.
 */
export const AccountContainer = () => {
  return <div></div>;
};

AccountContainer.displayName = "AccountContainer";
